#include <unistd.h>

/**
 * _putchar - writes the character c
 *	to stdout.
 * @c: the character to be writen.
 *
 * Return: 1 on Success.
 * -1, on error.
 */

int _putchar(char c)
{
	return (1, &c, 1);
}
